# Prospective RQ1-v2 results

Status: **pending/unverified**. No RQ1-v2 job result is promoted.

The tracked repository contains contracts and preflight entry points, not a
training engine, dataset, checkpoint, or completed run. Authorized dataset and
checkpoint artifacts must be transferred outside GitHub and verified before
use. `result_manifest.json` remains empty until all six exact arm/seed job
receipts validate together.

The contract-only summarizer never promotes receipt scalar values. Even six
syntactically complete receipts remain `pending/unverified` until a later
tracked evaluator authenticates per-case outputs, crossed contrasts, and the
registered uncertainty analysis. No p-value or system superiority claim is
produced here.
